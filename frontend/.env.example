# Local dev — points to your local FastAPI server
VITE_API_URL=http://localhost:8000

# Production — set this to your Render backend URL in Netlify dashboard
# VITE_API_URL=https://upalpha-backend.onrender.com
